# [백준] 쇠막대기

1. 음하하하성공

```python
n=list(input().strip())
stack=[]
ans=0
for i in range(len(n)):
  if n[i]=='(':
    stack.append('(')
  else:
    if stack!=[]:
        stack.pop()
        if n[i-1]=='(':
            ans+=len(stack)
        else:
            ans+=1
print(ans)
```