# [프그] 숫자 변환하기

1. 87.5 점

```protobuf
from collections import deque
def solution(x, y, n):
    answer = -1
    cnt=0
    que=deque()
    que.append([x,0])
    v=[False]*(y+1)
    
    while que:
        tmp,d=que.popleft()
        v[tmp]=True
        
        if tmp==y:
            return d
        
        
        if tmp+n <=y and v[tmp+n]==False: 
            que.append([tmp+n,d+1])
        if tmp*2 <= y and v[tmp*2]==False :
            que.append([tmp*2,d+1])
        if tmp*3 <=y and v[tmp*3]==False :
            que.append([tmp*3,d+1])
        
        
    return -1
```

1. 잘 봐야함 true를 그냥 해줘야함 !! 

```protobuf
from collections import deque
def solution(x, y, n):
    answer = -1
    cnt=0
    que=deque()
    que.append([x,0])
    v=[False]*(y+1)
    v[x]=True
    while que:
        tmp,d=que.popleft()
        
        if tmp==y:
            return d
        if tmp*3 <=y and v[tmp*3]==False :
            v[tmp*3]=True
            que.append([tmp*3,d+1])
            if tmp*3==y:
                return d+1
        if tmp*2 <= y and v[tmp*2]==False :
            que.append([tmp*2,d+1])
            v[tmp*2]=True
            if tmp *2 ==y:
                return d+1
        
        if tmp+n <=y and v[tmp+n]==False: 
            que.append([tmp+n,d+1])
            v[tmp+n]=True
            if tmp+n==y:
                return d+1
        
        
        
    return -1
```